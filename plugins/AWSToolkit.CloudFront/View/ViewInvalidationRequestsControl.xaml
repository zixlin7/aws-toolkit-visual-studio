<commonui:BaseAWSView x:Class="Amazon.AWSToolkit.CloudFront.View.ViewInvalidationRequestsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commonui="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"
    Height="300" Width="500">
    
    <commonui:BaseAWSView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AWSToolkit;component/Themes/DefaultTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </commonui:BaseAWSView.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*"/>

        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Grid.Column="0" ToolBarTray.IsLocked="True" >
            <Button Click="onRefreshClick">
                <Button.Content>
                    <WrapPanel>
                        <Image Width="16" Height="16" DataContext="{DynamicResource CommonIcons}" Source="{Binding RefreshIcon}"/>
                        <TextBlock>Refresh</TextBlock>
                    </WrapPanel>
                </Button.Content>
            </Button>
            <TextBlock>Requests:</TextBlock> 
            <ComboBox Margin="5,0,5,0" Name="_ctlRequestPicker" ItemsSource="{Binding Summaries}" DisplayMemberPath="DisplayName" Width="200" 
                      SelectionChanged="onSelectionChanged" 
                      Style="{DynamicResource awsComboBoxStyle}"/>
            <TextBlock>Created:</TextBlock>
            <TextBlock Margin="5,0,5,0" Name="_ctlCreateTime" Style="{DynamicResource awsTextBlockBaseStyle}"/>
        </ToolBar>
        <commonui:ToolkitDataGridWrapper AutoGenerateColumns="False" Grid.Row="1" Grid.Column="0" x:Name="_ctlDetailsGrid" CellStyle="{DynamicResource awsDataGridCellCentering}"
            CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="True"
            ItemsSource="{Binding Paths}" 
            RowHeaderWidth="0"
            HorizontalAlignment="Stretch" HeadersVisibility="All">

            <commonui:ToolkitDataGridWrapper.Columns>
                <DataGridTemplateColumn Width="auto" Header="Path">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <Image Source="{Binding Icon}" Width="16" Height="16"/>
                                <TextBlock Margin="5,0,5,0" Text="{Binding Path}"/>
                            </WrapPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </commonui:ToolkitDataGridWrapper.Columns>
        </commonui:ToolkitDataGridWrapper>
    </Grid>
</commonui:BaseAWSView>
