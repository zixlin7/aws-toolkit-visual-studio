<commonui:BaseAWSControl x:Class="Amazon.AWSToolkit.IdentityManagement.View.Components.AccessKeyDetailsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commonui="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"
    Height="130" Width="450">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AWSToolkit;component/Themes/DefaultTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Margin="5,5,5,5" Grid.Row="0" Grid.Column="0" Text="Access Key ID:" Style="{DynamicResource awsTextBlockBaseStyle}" />
        <TextBox Margin="5,5,5,5" Grid.Row="0" Grid.Column="1" Style="{DynamicResource awsSelectableTextBoxStyle}" Text="{Binding AccessKey}"/>

        <TextBlock Margin="5,5,5,5" Grid.Row="1" Grid.Column="0" Text="Secret Access Key:" Style="{DynamicResource awsTextBlockBaseStyle}"/>
        <TextBox Margin="5,5,5,5" Grid.Row="1" Grid.Column="1" Style="{DynamicResource awsSelectableTextBoxStyle}" Text="{Binding SecretKey}"/>

        <CheckBox Margin="5,10,5,5" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
                  IsChecked="{Binding PersistSecretKeyLocal}"
                  Style="{DynamicResource awsCheckboxButtonBaseStyle}"
                  >
            <TextBlock TextWrapping="Wrap"
                       Style="{DynamicResource awsTextBlockBaseStyle}">
                Save the secret access key locally.  AWS only returns the secret access key when created.
            </TextBlock>
        </CheckBox>
    </Grid>
</commonui:BaseAWSControl>
