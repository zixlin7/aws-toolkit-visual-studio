<commonUi:BaseAWSControl x:Class="Amazon.AWSToolkit.Publish.Views.ReenableOldPublishDialog"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:local="clr-namespace:Amazon.AWSToolkit.Publish.Views"
                         xmlns:commonUi="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"
                         xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes;assembly=AWSToolkit"
                         themes:ToolkitThemes.UseToolkitTheme="True"
                         mc:Ignorable="d" 
                         Height="175" Width="500">
    <UserControl.Resources>
        <ResourceDictionary>
            <commonUi:VsImages x:Key="VsImages" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="0 0 11 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image 
            Grid.Row="0" 
            Grid.Column="0"
            Margin="16 0 20 0"
            Width="32"
            Height="32"
            Source="{Binding Source={StaticResource VsImages}, Path=StatusInformation32}" 
            />

        <StackPanel
            Grid.Row="0" 
            Grid.Column="1"
            Orientation="Vertical"
            VerticalAlignment="Center"
            >
            <TextBlock 
                Grid.Row="0" 
                Grid.Column="1"
                Text="You can now access both the new and previous AWS publishing experiences from the context menu for a project in the Solution Explorer." 
                TextWrapping="WrapWithOverflow"
                />
            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0 12 0 0" TextWrapping="WrapWithOverflow">
                To help us improve the new AWS publishing experience, please 
                <Hyperlink FontWeight="SemiBold" Command="{Binding FeedbackCommand}"
                           CommandParameter="Publish To AWS" >
                    share feedback
                </Hyperlink>.
            </TextBlock>
        </StackPanel>
    </Grid> 
</commonUi:BaseAWSControl>
