<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes;assembly=AWSToolkit"
                    xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">

    <Style
        x:Key="PublishButtonStyle"
        TargetType="Button"
        BasedOn="{StaticResource {x:Static themes:ToolkitThemeResourceKeys.ButtonStyle}}">
        <Setter Property="MinWidth" Value="75" />
        <Setter Property="MinHeight" Value="23" />
        <Setter Property="Padding" Value="6 8 6 8" />
        <Setter Property="Content" Value="Publish" />
        <Style.Triggers>
            <!-- Style the button to look like a "Default" button -->
            <Trigger Property="IsMouseOver" Value="False">
                <Setter Property="Background"
                        Value="{DynamicResource {x:Static platformUi:CommonControlsColors.ButtonDefaultBrushKey}}" />
                <Setter Property="BorderBrush"
                        Value="{DynamicResource {x:Static platformUi:CommonControlsColors.ButtonBorderDefaultBrushKey}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
