<UserControl x:Class="Amazon.AWSToolkit.Publish.Views.Properties.FilePathTypeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes;assembly=AWSToolkit"
             themes:ToolkitThemes.UseToolkitTheme="True"
             mc:Ignorable="d"
             d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source=".\PropertyEditors.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel 
        LastChildFill="True">
        <Button
            DockPanel.Dock="Right"
            Margin="6 0 0 0"
            Content="Browse" 
            ToolTip="Select a file"
            Command="{Binding BrowseCommand}"
        />
        <ContentControl 
            IsEnabled="true"
            IsTabStop="False"
            Content="{Binding}"
            ContentTemplate="{StaticResource TextEditor}"
        />
    </DockPanel>
</UserControl>
