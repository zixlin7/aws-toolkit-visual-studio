using System.Windows.Controls;

namespace Amazon.AWSToolkit.Publish.Views
{
    /// <summary>
    /// This is the view that is shown when users elect to publish to a new target
    /// (opposed to re-publishing to an existing target).
    /// </summary>
    public partial class NewTargetDetailsView : UserControl
    {
        public NewTargetDetailsView()
        {
            InitializeComponent();
        }
    }
}
