<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:AwsToolkit.VsSdk.Common.CommonUI"
    xmlns:platformUi="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <!-- https://learn.microsoft.com/en-us/visualstudio/extensibility/ux-guidelines/layout-for-visual-studio?view=vs-2022#BKMK_ThemedDialogLayout -->
    <Style TargetType="{x:Type local:ThemedDialogWindow}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ThemedDialogWindow}">
                    <Grid>
                        <Border
                            Background="{Binding Background, RelativeSource={RelativeSource TemplatedParent}}"
                            BorderThickness="1"
                            BorderBrush="{DynamicResource {x:Static platformUi:ThemedDialogColors.WindowBorderBrushKey}}">
                            <DockPanel LastChildFill="True">
                                <!-- VS would use a GlyphButton in this case, but it's not available for extensions.
                                See ThemedDialogWindow.OnApplyTemplate override for how this button event handler is wired. -->
                                <Button
                                    DockPanel.Dock="Top"
                                    Content="&#x2715;"
                                    Foreground="{DynamicResource {x:Static platformUi:ThemedDialogColors.WindowButtonGlyphBrushKey}}"
                                    IsCancel="true"
                                    IsEnabled="{Binding IsCloseButtonEnabled, RelativeSource={RelativeSource AncestorType={x:Type local:ThemedDialogWindow}}}"
                                    HorizontalAlignment="Right"
                                    Margin="9 6 9 6"
                                    MinHeight="0"
                                    MinWidth="0">
                                    <!-- Bare bones template to get rid of border, triggers, and other default button stuff -->
                                    <Button.Template>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <ContentPresenter />
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <ContentPresenter
                                    Margin="24 0 24 24" />
                            </DockPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="HasMaximizeButton" Value="False" />
        <Setter Property="HasMinimizeButton" Value="False" />
        <Setter Property="ResizeMode" Value="CanResizeWithGrip" />
        <Setter Property="ShowInTaskbar" Value="False" />

        <!-- https://learn.microsoft.com/en-us/dotnet/api/system.windows.shell.windowchrome?view=netframework-4.7.2
        CornerRadius requires GlassFrameThickness="0" -->
        <Setter Property="shell:WindowChrome.WindowChrome">
            <Setter.Value>
                <shell:WindowChrome
                    CaptionHeight="0"
                    CornerRadius="0"
                    GlassFrameThickness="0"
                    ResizeBorderThickness="5"
                    UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
