<UserControl x:Class="Amazon.AWSToolkit.CloudWatch.Views.SearchBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:commonUi="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"
             xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes;assembly=AWSToolkit"
             xmlns:images="clr-namespace:Amazon.AWSToolkit.CommonUI.Images;assembly=AWSToolkit"
             themes:ToolkitThemes.UseToolkitTheme="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <commonUi:VsImages x:Key="VsImages" />
            <Style x:Key="SearchBoxStyle" TargetType="{x:Type DockPanel}">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Text, ElementName=SearchTermTextBox}" Value="">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchTermTextBox"
                 Text="{Binding FilterText , UpdateSourceTrigger=PropertyChanged, Delay=500}"
                 Background="Transparent"
                 HorizontalAlignment="Stretch" />

        <DockPanel Style="{StaticResource SearchBoxStyle}">
            <TextBlock
                Text="{Binding HintText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                DockPanel.Dock="Left"
                IsHitTestVisible="False"
                Margin="10,0,0,0"
                Foreground="{DynamicResource {x:Static themes:ToolkitThemes.HintTextBrushKey}}" />

            <images:VsImage
                Width="16"
                Height="16"
                HorizontalAlignment="Right"
                DockPanel.Dock="Right"
                Margin="0,0, 5, 0"
                Source="{Binding Source={StaticResource VsImages}, Path=Search}" />
        </DockPanel>
    </Grid>
</UserControl>
