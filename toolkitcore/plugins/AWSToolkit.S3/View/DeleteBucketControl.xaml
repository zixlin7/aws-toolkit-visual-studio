<commonui:BaseAWSControl x:Class="Amazon.AWSToolkit.S3.View.DeleteBucketControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:commonui="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"  
             Height="170" Width="400">
    
    <commonui:BaseAWSControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AWSToolkit;component/Themes/DefaultTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </commonui:BaseAWSControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="WrapWithOverflow" Margin="5"  Name="txtConfirmationMessage"
                   Text="{Binding BucketName, StringFormat=Are you sure you want to delete the {0} bucket?}"
                   Style="{DynamicResource awsTextBlockBaseStyle}">            
        </TextBlock>
        <TextBlock Grid.Row="1" TextWrapping="WrapWithOverflow" Margin="5" FontStyle="Italic"  Style="{DynamicResource awsFieldHintTextStyle}">
        All objects (including all object versions and Delete Markers) in the bucket must be deleted before the bucket itself can be deleted.
        </TextBlock>
        <CheckBox Margin="10" Grid.Row="2" IsChecked="{Binding DeleteBucketWithObjects}" Style="{DynamicResource awsCheckboxButtonBaseStyle}">
            <TextBlock TextWrapping="WrapWithOverflow" Style="{DynamicResource awsTextBlockBaseStyle}">
             Delete all objects (If the bucket contains a large number of objects, this operation will take a long time to complete. The delete process will be interrupted if you close Visual Studio before it completes.)
            </TextBlock>
        </CheckBox>
    </Grid>
</commonui:BaseAWSControl>
