<commonUi:BaseAWSControl x:Class="Amazon.AWSToolkit.ECS.View.ContainerSelectionControl"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                         xmlns:commonUi="clr-namespace:Amazon.AWSToolkit.CommonUI;assembly=AWSToolkit"
                         xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes;assembly=AWSToolkit"
                         themes:ToolkitThemes.UseToolkitTheme="True"
                         mc:Ignorable="d"
                         Height="200" Width="400"
                         d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Margin="5"
                   TextWrapping="WrapWithOverflow"
                   FontWeight="SemiBold"
                   Text="Select a container to view associated CloudWatch Logs." />
        <Label Content="Container:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
        <ComboBox
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            VerticalContentAlignment="Center"
            MinWidth="100"
            Grid.Row="1" Grid.Column="1"
            HorizontalContentAlignment="Stretch"
            Margin="5"
            ItemsSource="{Binding Containers,  RelativeSource={RelativeSource AncestorType=UserControl}}"
            SelectedItem="{Binding Container,  RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </Grid>
</commonUi:BaseAWSControl>
