<UserControl x:Class="Amazon.AWSToolkit.Publish.Banner.PublishBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:themes="clr-namespace:Amazon.AWSToolkit.Themes"
             themes:ToolkitThemes.UseToolkitTheme="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AWSToolkit;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

            <Style TargetType="Button" BasedOn="{StaticResource {x:Static themes:ToolkitThemeResourceKeys.ButtonStyle}}">
                <Setter Property="MinWidth" Value="75" />
                <Setter Property="Height" Value="23" />
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border 
        Padding="5" 
        BorderThickness="1"
        BorderBrush="{DynamicResource {x:Static themes:ToolkitThemes.ToolTipBorderBrushKey}}" 
        Background="{DynamicResource {x:Static themes:ToolkitThemes.ToolTipBackgroundBrushKey}}"
        Visibility="{Binding ShowBanner, Converter={StaticResource BooleanToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged}">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label
                    Foreground="{DynamicResource {x:Static themes:ToolkitThemes.ToolTipTextBrushKey}}"
                    FontWeight="Bold"
                    Content="NEW: Preview the re-imagined AWS Publishing experience" />

                <Label 
                    Foreground="{DynamicResource {x:Static themes:ToolkitThemes.ToolTipTextBrushKey}}"
                    Content="We've streamlined the steps for publishing your applications to AWS.&#10;Try it out and let us know what you think!" />
            </StackPanel>

            <StackPanel 
                Grid.Column="2"
                Margin="0 0 12 0"
                Orientation="Vertical"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                >
                <TextBlock Margin="0 0 0 9" HorizontalAlignment="Center">
                    <Hyperlink Command="{Binding LearnMoreCommand}">Learn more</Hyperlink>
                </TextBlock>
                <TextBlock HorizontalAlignment="Center">
                    <Hyperlink Command="{Binding SwitchToNewExperienceCommand}">Try new experience</Hyperlink>
                </TextBlock>
            </StackPanel>

        </Grid>
    </Border>
</UserControl>
